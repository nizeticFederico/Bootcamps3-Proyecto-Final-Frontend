import React from 'react';
import PreviewEvent from '@/components/UI/PreviewEvent';

const ViewEvent = () => {
    return (
      <div>
        <PreviewEvent />
      </div>
    );
};

export default ViewEvent;